# Copy to /etc/snarkyctl/targets.yaml. The browser submits aliases only.
# provider_target is an opaque value interpreted only by the selected adapter.
schema_version: 1

targets:
  - alias: dallas
    label: Dallas, United States
    provider_target: Dallas
  - alias: prague
    label: Prague, Czechia
    provider_target: Prague
